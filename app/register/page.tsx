import AuthArea from "../../components/AuthArea";

export const metadata = {
  title: "Create Account | Worldstreet",
};

export default function RegisterPage() {
  return <AuthArea mode="register" />;
}
